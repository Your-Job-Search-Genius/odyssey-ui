"use client";

import { Paragraph } from "@/components/base/typography/paragraph";

export const Default = () => (
    <Paragraph className="max-w-md">
        Odyssey is a component library built on React Aria Components, giving every component consistent accessibility and behavior out of the box.
    </Paragraph>
);
